{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "R-Debugger",
        "request": "launch",
        "name": "R: Debug Current File",
        "debugMode": "file",
        "workingDirectory": "${fileDirname}"
      }
    ]
}